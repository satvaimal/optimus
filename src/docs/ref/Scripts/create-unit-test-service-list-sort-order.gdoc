h1. create-unit-test-service-list-sort-order

h2. Purpose

The @create-unit-test-service-list-sort-order@ generates all the unit tests for the service @list@ method and @sort@ and @order@ parameter.

h2. Examples

{code:java}
grails create-unit-test-service-list-sort-order Person
grails create-unit-test-service-list-sort-order "*"
{code}

h2. Description

Given at least one domain class, this command creates unit tests for the service @list@ method and @sort@ and @order@ parameter. The command will generate the following file:
* @test/unit/{package}/{domainClass}ServiceListSortOrderTests.groovy@

Usage:

{code:java}
grails create-unit-test-service-list-sort-order [domainClass]
{code}

Arguments:
* @domainClass@ - Either a domain class name (case-sensitive) without its package or a wildcard (*). If you specify the wildcard the artifacts will be generated for all domain classes.
