h1. create-unit-test-service-delete

h2. Purpose

The @create-unit-test-service-delete@ generates all the unit tests for the service @delete@ method.

h2. Examples

{code:java}
grails create-unit-test-service-delete Person
grails create-unit-test-service-delete "*"
{code}

h2. Description

Given at least one domain class, this command creates unit tests for the service @delete@ method. The command will generate the following file:
* @test/unit/{package}/{domainClass}ServiceDeleteTests.groovy@

Usage:

{code:java}
grails create-unit-test-service-delete [domainClass]
{code}

Arguments:
* @domainClass@ - Either a domain class name (case-sensitive) without its package or a wildcard (*). If you specify the wildcard the artifacts will be generated for all domain classes.
