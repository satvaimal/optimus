h1. create-service-delete-log

h2. Purpose

The @create-service-delete-log@ generates all the logs for the service @delete@ method.

h2. Examples

{code:java}
grails create-service-delete-log Person
grails create-service-delete-log "*"
{code}

h2. Description

Given at least one domain class, this command creates logs for the service @delete@ method. The command will generate the following file:
* @src/groovy/{package}/aop/{domainClass}ServiceDelete.groovy@

Usage:

{code:java}
grails create-service-delete-log [domainClass]
{code}

Arguments:
* @domainClass@ - Either a domain class name (case-sensitive) without its package or a wildcard (*). If you specify the wildcard the artifacts will be generated for all domain classes.

