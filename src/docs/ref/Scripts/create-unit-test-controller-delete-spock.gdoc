h1. delete-unit-test-controller-delete-spock

h2. Purpose

The @delete-unit-test-controller-delete-spock@ generates all the unit tests for the controller @delete@ method with the "Spock Framework":https://code.google.com/p/spock/

h2. Examples

{code:java}
grails delete-unit-test-controller-delete-spock Person
grails delete-unit-test-controller-delete-spock "*"
{code}

h2. Description

Given at least one domain class, this command creates unit tests for the controller @delete@ method with the "Spock Framework":https://code.google.com/p/spock/ . The command will generate the following file:
* @test/unit/{package}/{domainClass}ServiceDeleteSpec.groovy@

Usage:

{code:java}
grails delete-unit-test-controller-delete-spock [domainClass]
{code}

Arguments:
* @domainClass@ - Either a domain class name (case-sensitive) without its package or a wildcard (*). If you specify the wildcard the artifacts will be generated for all domain classes.
